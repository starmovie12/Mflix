/**
 * Shared types - re-export from tmdb types for backward compatibility.
 */

export type {
  TMDBVideo,
  TMDBMovie,
  TMDBMovieDetails,
  MovieRowData,
  HeroMovie,
  WatchlistMovie,
} from "./tmdb/types";
