import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'MFLIX - Watch Movies & TV Shows Online',
  description: 'Stream unlimited movies and TV shows. Your next favorite story is here.',
  manifest: '/manifest.json',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased bg-pitch text-white min-h-screen">
        {children}
      </body>
    </html>
  );
}
